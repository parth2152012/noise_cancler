[package]
name = "noise-cancler"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "No-std derivative filtering for IMU vibration mitigation"

[lib]
path = "src/lib.rs"

[profile.release]
codegen-units = 1
lto = true
panic = "abort"
